<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>You're invited to a new role</title>
  <style>
    body { font-family: Arial, Helvetica, sans-serif; background:#f6f9fc; color:#0f172a; }
    .container { max-width: 560px; margin: 24px auto; background: #ffffff; border-radius: 8px; padding: 24px; border:1px solid #e5e7eb; }
    .btn { display:inline-block; padding:12px 16px; background:#2563eb; color:#fff; text-decoration:none; border-radius:6px; }
    .muted { color:#64748b; font-size: 12px; }
  </style>
</head>
<body>
  <div class="container">
    <h2>You're invited to join as {{ role }}</h2>
    <p>Hello {{ username }},</p>
    <p>You've been invited to join our system with a new role: <strong>{{ role }}</strong></p>
    <p>Please click the button below to accept this invitation:</p>
    <p>
      <a class="btn text-white" href="{{ action_url }}" target="_blank" rel="noopener">Accept Invitation</a>
    </p>
    <p class="muted">If the button doesn't work, copy and paste this link into your browser:<br />
      <a href="{{ action_url }}">{{ action_url }}</a>
    </p>
    <p>This invitation will allow you to access additional features and responsibilities within our system.</p>
  </div>
</body>
</html>
